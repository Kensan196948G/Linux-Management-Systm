Webmin風Linux管理WebUI（ClaudeCodeにて開発）詳細要件定義書、詳細設計仕様書

* 📘 **詳細要件定義書**
* 🛠 **詳細設計仕様書**

を **そのまま社内文書として提出可能な体裁**でまとめます。
（※ Webmin互換ではなく「Webmin思想を踏襲した安全設計」が前提です）

---

# 📘 詳細要件定義書

Webmin風 Linux管理WebUI
（ClaudeCode 開発前提）

---

## 1. 文書概要

| 項目     | 内容                               |
| ------ | -------------------------------- |
| 文書名    | Linux管理WebUI 要件定義書               |
| 想定システム | Webminライク Linux運用管理WebUI         |
| 開発方式   | ClaudeCode 主導（人間レビュー併用）          |
| 利用環境   | Linux（本番）／社内ネットワーク               |
| 想定利用者  | IT部門運用担当者                        |
| 想定目的   | Linuxサーバの**安全なWebUI管理・可視化・操作統制** |

---

## 2. 背景・目的

### 2.1 背景

* Webminは便利だが、

  * 権限制御が粗い
  * 監査ログ・承認フローが弱い
  * 組織運用に最適化されていない
* 社内IT運用では
  **「できること」より「やってはいけないこと」** が重要。

### 2.2 目的

* root作業を **WebUI経由で安全に制御**
* sudo権限を **最小化・可視化**
* 操作ログを **完全に証跡化**
* 将来的な **ITSM・承認フロー連携** を前提とする

---

## 3. システム全体像

![Image](https://img.kaspersky.com/oh/KSCLinux/14.2/en-US/loc_scheme_architecture_ksc_nwc.png)

![Image](https://www.researchgate.net/publication/26501480/figure/fig3/AS%3A669970841235466%401536744904839/Wrapper-architecture-and-interfaces.png)

![Image](https://mojasim-storage.s3.ap-south-1.amazonaws.com/1769324545654-The-Best-way-To-Secure-Your-Linux-Server.png)

```
[ Browser ]
   ↓ HTTPS
[ WebUI (HTML/JS) ]
   ↓ API
[ Backend API (FastAPI/Flask) ]
   ↓ sudo (allowlist)
[ Root Wrapper Scripts ]
   ↓
[ Linux OS / systemctl / journalctl ]
```

---

## 4. 機能要件

### 4.1 機能分類

| 区分     | 内容         |
| ------ | ---------- |
| 参照系    | 状態確認・ログ閲覧  |
| 操作系    | 限定的なroot操作 |
| 管理系    | ユーザー・権限・監査 |
| セキュリティ | 認証・認可・ログ   |
| 拡張     | ITSM・承認連携  |

---

### 4.2 参照系機能（必須）

| 機能     | 内容               |
| ------ | ---------------- |
| システム状態 | CPU / メモリ / ディスク |
| サービス状態 | systemctl status |
| プロセス   | ps / top（参照のみ）   |
| ログ閲覧   | journalctl（条件指定） |
| ネットワーク | NIC/IP 状態表示      |

---

### 4.3 操作系機能（限定）

| 操作      | 制御方針        |
| ------- | ----------- |
| サービス再起動 | 許可サービスのみ    |
| サービス停止  | 原則禁止（承認時のみ） |
| ログローテ確認 | 参照のみ        |
| cron 確認 | 参照のみ        |

❌ **禁止**

* 任意コマンド実行
* bash / sh 起動
* /etc 直接編集
* ユーザー・sudo追加

---

### 4.4 管理・監査機能

| 機能   | 内容              |
| ---- | --------------- |
| 操作ログ | 誰が・いつ・何を        |
| 実行結果 | stdout / stderr |
| 操作区分 | 参照 / 実行         |
| ログ保全 | 追記専用・改ざん防止      |
| 監査出力 | CSV / JSON      |

---

## 5. 非機能要件

### 5.1 セキュリティ要件

| 項目         | 要件          |
| ---------- | ----------- |
| root常駐     | 禁止          |
| sudo       | allowlist必須 |
| shell=True | 禁止          |
| 引数検証       | 全操作必須       |
| コマンド制限     | ラッパー経由のみ    |

---

### 5.2 可用性・運用

| 項目     | 内容         |
| ------ | ---------- |
| タイムアウト | 全コマンド      |
| 同時実行   | 制限あり       |
| エラー    | UIに表示＋ログ   |
| 再起動    | systemd 管理 |

---

## 6. 拒否基準（最重要）

### 6.1 デフォルト拒否ポリシー

* **定義されていない操作は全拒否**
* UI入力は **選択式のみ**
* 文字入力はフィルタ必須

### 6.2 即時拒否文字

```
; | & $( ) ` > < * ? { } [ ]
```

---

# 🛠 詳細設計仕様書

---

## 7. ユーザー・権限設計

### 7.1 ユーザーロール

| ロール      | 権限     |
| -------- | ------ |
| Viewer   | 参照のみ   |
| Operator | 限定操作   |
| Approver | 危険操作承認 |
| Admin    | 設定管理   |

---

## 8. sudo / root 制御設計（核心）

### 8.1 実行ユーザー

* Linuxユーザー：`svc-adminui`
* Backend は root では起動しない

### 8.2 sudoers 設計思想

* **直接コマンドは禁止**
* **必ずラッパー経由**

#### 許可例（思想）

```
svc-adminui ALL=(root) NOPASSWD: /usr/local/sbin/adminui-*
```

---

## 9. ラッパー設計

### 9.1 ラッパーの役割

* 入力検証
* 許可操作判定
* 実コマンド実行
* 結果返却

### 9.2 設計ルール

* 引数は配列
* shell 展開なし
* 実行前ログ
* 実行後ログ

---

## 10. API設計（概要）

| API                       | 内容         |
| ------------------------- | ---------- |
| GET /api/status           | システム状態     |
| GET /api/services         | サービス一覧     |
| POST /api/service/restart | 再起動        |
| GET /api/logs             | journalctl |

---

## 11. エラーハンドリング

| 種別     | 対応       |
| ------ | -------- |
| sudo拒否 | UI警告＋ログ  |
| タイムアウト | 強制終了     |
| 実行失敗   | stderr表示 |
| 予期せぬ操作 | 即遮断      |

---

## 12. ClaudeCode 利用方針

### 12.1 ClaudeCodeの役割

* 要件→設計→コード生成
* セキュリティレビュー
* ログ整合性チェック
* ドキュメント自動生成

### 12.2 人間レビュー必須ポイント

* sudoers変更
* 新規操作追加
* 承認フロー変更

---

## 13. フェーズ別開発計画（推奨）

| フェーズ | 内容    |
| ---- | ----- |
| v0.1 | 参照系のみ |
| v0.2 | 限定操作  |
| v0.3 | 承認フロー |
| v1.0 | 本番運用  |

---

## 14. 本設計の位置づけ

> 本システムは
> **「Linuxを操作するツール」ではなく**
> **「Linux運用を統制する仕組み」**
> である。


